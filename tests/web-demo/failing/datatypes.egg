(datatype*
    (Math
        (Add Math Math)
        (Sum MathVec)
        (B Bool))
    (sort MathVec (Vec Math))
    (Bool
        (True)
        (False)))

(let expr (Add (Sum (vec-of (B (True)) (B (False)))) (B (True))))
