
(datatype Math
  (Add i64 i64)
  (Num i64))


(rule ((Add a b)) ((union (Add a b) (Add b a))) :name "rw1")
(rule ((Add a b)
       (> a b))
      ((union (Add a b) (Num (+ a b)))) :name "rw2")


(let start (Add 2 3))

(run 2)
(prove (= (Add 2 3) (Num 5)))